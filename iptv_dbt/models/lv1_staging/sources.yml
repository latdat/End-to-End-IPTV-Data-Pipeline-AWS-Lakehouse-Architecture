version: 2
#Khai báo sources, dữ liệu thô cho tầng staging. 
sources:
  - name: staging
    database: dev
    schema: staging
    tables:
      - name: viewing_history
        description: "Dữ liệu thô từ S3 Silver. Chấp nhận dữ liệu rác để quan sát."
        columns:
          - name: event_id
            description: "Chỉ cảnh báo nếu trùng ở tầng thô"
            tests:
              - unique:
                  config:
                    severity: warn 
          - name: total_duration_seconds
            description: "Không đặt test ở đây vì dữ liệu thô chắc chắn có số âm"