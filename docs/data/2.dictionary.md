# Từ điển dữ liệu (Data Dictionary) — IPTV Viewing History

> **Đường ống (Pipeline):** Landing -> Bronze -> Silver -> Gold (Redshift + dbt)  
> **Medallion layers:** Bronze · Silver · Gold  
> **Chủ sở hữu (Owner):** Phuc Vinh
> **Cập nhật (Updated):** 12/04/2026  

---

## Các quy ước (Conventions)

**Nhãn vai trò (Role labels)** dùng trong cột "Role":

| Label | Ý nghĩa |
|---|---|
| PK | Khóa chính / surrogate key |
| FK | Khóa ngoại / join column |
| derived | Cột tính toán / derived column |
| flag | Cờ chỉ báo (Boolean indicator) |
| dropped | Cột bị loại bỏ (drop) ở layer này |
| new | Mới xuất hiện ở layer này |

**Kiểu dữ liệu:**

| Viết tắt | Kiểu thực tế |
|---|---|
| str | STRING / VARCHAR |
| long | BIGINT (64-bit integer) |
| int | INTEGER (32-bit) |
| dbl | DOUBLE / FLOAT8 |
| bool | BOOLEAN |
| date | DATE (yyyy-MM-dd) |
| ts | TIMESTAMP (UTC) |

---

## Tầng 1 (Layer 1) — Bronze

**Nguồn (Source):** S3 Landing (`s3://.../landing/*.json`)  
**Công cụ (Tool):** AWS Glue (read) -> S3 Bronze (Parquet)  
**Biến đổi (Transformation):** Trải phẳng (Flatten) nested JSON (`_source.*`), parse `date` từ filename, drop metadata columns không cần thiết.  
**Mục tiêu (Goal):** Lưu trữ trung thực dữ liệu gốc sau khi flatten — không lọc (filter), không sửa giá trị.  
**Định dạng đầu ra (Format output):** Parquet, partitioned by `batch_date`  
**Số lượng bản ghi (Row count) (tháng 4/2022):** 48,457,499

| Tên cột | Vai trò (Role) | Kiểu (Type) | Nullable | Nguồn gốc | Mô tả | Ví dụ |
|---|---|---|---|---|---|---|
| `_id` | PK | str(20) | NO | `_id` | Unique ID của sự kiện xem (viewing event). | `"xYz12abc34DEF56ghi7"` |
| `Contract` | FK | str | YES | `_source.Contract` | Mã hợp đồng thuê bao.<br>Chứa cả giá trị hợp lệ & rác (garbage). | `"HNH236275"`, `"0"`, `"113.182.209.48"` |
| `Mac` | — | str | NO | `_source.Mac` | Địa chỉ MAC của thiết bị.<br>Định dạng: 12-hex liền hoặc colon-separated. | `"001C55007900"`, `"00:1C:55:00:79:00"` |
| `TotalDuration` | — | long | NO | `_source.TotalDuration` | Tổng thời gian xem (giây).<br>Chứa cả giá trị âm và zero. | `6702`, `0`, `-33194` |
| `AppName` | — | str | NO | `_source.AppName` | Tên ứng dụng / kênh (Uppercase).<br>9 giá trị khác biệt. | `"CHANNEL"`, `"VOD"`, `"CHILD"` |
| `date` | — | date | NO | Filename | Ngày dữ liệu export (từ pattern `yyyyMMdd`).<br>Dùng làm partition key. | `2022-04-01` |
| `_index` | dropped | str | — | `_index` | Hằng số `"history"`. Loại bỏ (Drop) tại Glue. | `"history"` |
| `_type` | dropped | str | — | `_type` | Bản sao chữ thường của `AppName`.<br>Dư thừa (Redundant). | `"channel"`, `"vod"` |

**Chi tiết bổ sung (Bronze Layer):**
- **`_id`**: Do Elasticsearch tạo ra. Dài đúng 20 ký tự `[A-Za-z0-9_\-]`. Không có sự kiện trùng lặp (duplicate) ở nguồn (source).
- **`Contract`**: Chưa được làm sạch (clean) ở layer này, có thể chứa `"0"`, `"PAYTV"`, IP address, lowercase.
- **`Mac`**: Chưa chuẩn hóa (normalize).
- **`TotalDuration`**: Giá trị âm có thể do lệch đồng hồ (clock skew). Phạm vi (range) thực tế: `-889,575,165` -> `86,400`. Chưa lọc (filter) ở layer này.
- **`AppName`**: Gồm cả `"BHD"` (sự kiện kiểm thử/test, 1 record).
- **`date`**: Không có trong raw JSON.

**Lược đồ phân vùng (Partition scheme):** `batch_date=2022-04-01/`, `batch_date=2022-04-02/`, ...

---

## Tầng 2 (Layer 2) — Silver

**Nguồn (Source):** S3 Bronze  
**Công cụ (Tool):** AWS Glue Job (PySpark)  
**Biến đổi (Transformations):**
- Lọc (Filter) `duration_seconds`: giữ `0 < x <= 86,400`
- Lọc (Filter) `app_name != 'BHD'`
- Lọc (Filter) `contract_id` hợp lệ: regex `^[A-Z]{2,4}\d+$`; drop NULL, `"0"`, `"PAYTV"`, IP address
- Chuẩn hóa (Normalize) `mac_address` -> colon-separated uppercase `XX:XX:XX:XX:XX:XX`; drop 28 dòng không parse được
- Loại bỏ trùng lặp (Dedup) theo `event_id` (window: keep first)
- Phát hiện gian lận (Fraud detection): đánh dấu (flag) contract có số MAC khác biệt (distinct) >= ngưỡng (threshold) -> `is_fraud_suspect = TRUE`

**Định dạng đầu ra (Format output):** Parquet, partitioned by `batch_date`  
**Số bản ghi (Row count) (ước tính tháng 4/2022):** ~45,900,000 (~94.7% giữ lại/retention)

| Tên cột | Vai trò (Role) | Kiểu (Type) | Nullable | Nguồn gốc | Mô tả | Ví dụ |
|---|---|---|---|---|---|---|
| `event_id` | PK | str(20) | NO | Bronze (`_id`) | Giữ nguyên từ Bronze.<br>Đã loại bỏ trùng lặp (dedup). | `"xYz12abc34DEF56ghi7"` |
| `contract_id` | FK | str | NO | Bronze (`Contract`) | Chỉ giữ định dạng hợp lệ `^[A-Z]{2,4}\d+$`. | `"HNH236275"`, `"SGFDN0463"` |
| `device_mac` | — | str(17) | NO | Bronze (`Mac`) | Đã chuẩn hóa (normalize) thành format chuẩn.<br>Độ dài cố định 17 ký tự. | `"00:1C:55:00:79:00"` |
| `total_duration_seconds` | — | long | NO | Bronze (`TotalDuration`) | Đã lọc (filter): chỉ giữ `0 < duration <= 86,400`. | `6702`, `18322`, `86400` |
| `app_name` | — | str | NO | Bronze (`AppName`) | Uppercase. Đã loại bỏ `"BHD"`. | `"CHANNEL"` |
| `batch_date` | — | date | NO | Bronze (`date`) | Giữ nguyên. Partition key. | `2022-04-01` |
| `is_fraud_suspect` | new, flag | bool | NO | Computed | Cờ nghi ngờ gian lận (fraud suspect). | `false`, `true` |
| `glue_processed_at` | new | ts | NO | Computed | Timestamp UTC khi Glue job xử lý record này. | `2022-05-01T03:14:22Z` |

**Chi tiết bổ sung (Silver Layer):**
- **`event_id`**: Mỗi `event_id` xuất hiện đúng 1 lần.
- **`contract_id`**: Đã loại bỏ NULL, garbage values, IP address, lowercase.
- **`device_mac`**: Chuẩn hóa thành `XX:XX:XX:XX:XX:XX` (colon-separated, uppercase).
- **`total_duration_seconds`**: Đã loại bỏ 72,410 dòng âm/zero.
- **`app_name`**: 8 giá trị còn lại (CHANNEL, VOD, CHILD, KPLUS, RELAX, SPORT, FIMS).
- **`is_fraud_suspect`**: `TRUE` nếu `contract_id` này có số lượng `device_mac` khác biệt (distinct) >= ngưỡng gian lận (fraud threshold) trong toàn bộ dataset. Tính bằng hàm tổng hợp cửa sổ (window aggregation) trong Glue. Không drop record — giữ lại để kiểm toán (audit).
- **`glue_processed_at`**: Dùng để theo dõi vết lỗi (debug lineage) và kiểm toán (audit trail).

### Quy tắc nghiệp vụ (Business Rules) áp dụng tại Silver

| Rule ID | Condition | Action |
|---|---|---|
| BR-01 | `contract_id IS NULL` | DROP row |
| BR-02 | `contract_id NOT RLIKE '^[A-Z]{2,4}\d+$'` | DROP row |
| BR-03 | `total_duration_seconds <= 0` | DROP row |
| BR-04 | `total_duration_seconds > 86400` | DROP row |
| BR-05 | `app_name = 'BHD'` | DROP row |
| BR-06 | MAC không phân tích cú pháp (parse) được thành `XX:XX:XX:XX:XX:XX` | DROP row |
| BR-07 | `event_id` duplicate | DROP — giữ lần xuất hiện đầu tiên (first occurrence) |
| BR-08 | `contract_id` có distinct MAC >= threshold | SET `is_fraud_suspect = TRUE` |

---

## Tầng 3 (Layer 3) — Gold

Gold layer gồm 2 tầng dbt: Staging (view, 1-1 với Silver) và Mart (aggregate, materialized table).

---

### 3a. `stg_viewing_history` — Staging View

**Nguồn (Source):** Silver (via Redshift COPY từ S3 Silver Parquet)  
**Công cụ (Tool):** dbt-core 1.7 + dbt-redshift  
**Materialization:** `view`  
**Mục tiêu (Goal):** Thêm các cột dẫn xuất (derived columns) tiện dụng cho các mô hình luồng dữ liệu sau (downstream models) mà không thay đổi độ chi tiết (grain).  
**Grain:** 1 row = 1 viewing event (giống Silver)

| Tên cột | Vai trò (Role) | Kiểu (Type) | Nullable | Nguồn gốc | Mô tả | Ví dụ |
|---|---|---|---|---|---|---|
| `event_id` | PK | str(20) | NO | Silver | Giữ nguyên. | `"xYz12abc34DEF56ghi7"` |
| `contract_id` | FK | str | NO | Silver | Giữ nguyên. | `"HNH236275"` |
| `device_mac` | — | str(17) | NO | Silver | Giữ nguyên. | `"00:1C:55:00:79:00"` |
| `total_duration_seconds` | — | long | NO | Silver | Giữ nguyên. | `6702` |
| `total_duration_minutes` | derived | dbl | NO | Computed | `total_duration_seconds / 60.0`. Làm tròn 2 chữ số. | `111.70` |
| `app_name` | — | str | NO | Silver | Giữ nguyên. | `"CHANNEL"` |
| `batch_date` | — | date | NO | Silver | Giữ nguyên. Partition key. | `2022-04-01` |
| `view_year` | derived | int | NO | Computed | `EXTRACT(YEAR FROM batch_date)`.<br>Dùng cho time-based partitioning. | `2022` |
| `view_month` | derived | int | NO | Computed | `EXTRACT(MONTH FROM batch_date)`.<br>Range: 1-12. | `4` |
| `view_day` | derived | int | NO | Computed | `EXTRACT(DAY FROM batch_date)`.<br>Range: 1-31. | `1` |
| `is_fraud_suspect` | flag | bool | NO | Silver | Giữ nguyên từ Silver. | `false` |

**dbt model logic (tham khảo):**

```sql
SELECT
    event_id,
    contract_id,
    device_mac,
    total_duration_seconds,
    ROUND(total_duration_seconds / 60.0, 2)   AS total_duration_minutes,
    app_name,
    batch_date,
    EXTRACT(YEAR  FROM batch_date)::INT  AS view_year,
    EXTRACT(MONTH FROM batch_date)::INT  AS view_month,
    EXTRACT(DAY   FROM batch_date)::INT  AS view_day,
    is_fraud_suspect
FROM {{ source('silver', 'viewing_history') }}
```

---

### 3b. `mart.fct_daily_views` — Daily Aggregate Fact Table

**Nguồn (Source):** `stg_viewing_history`  
**Công cụ (Tool):** dbt-core 1.7 + dbt-redshift  
**Materialization:** `table` (incremental recommended — xem modeling.md)  
**Grain:** 1 row = 1 `(batch_date, app_name)` — mỗi app mỗi ngày  
**Số rows tối đa / ngày:** 8 (= số app_name khác biệt / distinct sau khi loại bỏ BHD)  
**Tổng rows (30 ngày):** tối đa 240 rows

| Tên cột | Vai trò (Role) | Kiểu (Type) | Nullable | Nguồn gốc | Định nghĩa nghiệp vụ (Business Definition) | Ví dụ |
|---|---|---|---|---|---|---|
| `pk` | PK | str | NO | Computed | Khóa thay thế (Surrogate key) MD5. | `"a1b2c3d4..."` |
| `batch_date` | — | date | NO | stg | Ngày của batch. | `2022-04-01` |
| `app_name` | — | str | NO | stg | Tên ứng dụng. | `"CHANNEL"` |
| `total_sessions` | derived | long | NO | `COUNT(*)` | Tổng số sự kiện xem (viewing events) theo ngày cho app (~sessions). | `43,747,334` |
| `unique_contracts` | derived | long | NO | `COUNT(DISTINCT contract_id)` | Số hợp đồng khác biệt (unique) có ít nhất 1 session. | `1,450,211` |
| `unique_devices` | derived | long | NO | `COUNT(DISTINCT device_mac)` | Số thiết bị (MAC address) khác biệt (unique) có ít nhất 1 session. | `1,654,822` |
| `total_seconds` | derived | long | NO | `SUM(...)` | Tổng giây xem tích lũy (raw). | `680197500` |
| `total_hours` | derived | dbl | NO | `SUM(...) / 3600.0` | Tổng giờ xem tích lũy (làm tròn 2 chữ số thập phân). | `188,943.75` |
| `avg_minutes_per_session` | derived | dbl | NO | `AVG(...) / 60.0` | Trung bình thời lượng mỗi session (phút). | `259.26` |

**Định nghĩa nghiệp vụ mở rộng (Mart Layer):**
- **`total_sessions`**: Bao gồm cả records nghi ngờ gian lận (`is_fraud_suspect = TRUE`). Luồng dữ liệu sau (downstream) tự lọc (filter) nếu cần.
- **`unique_contracts`**: Proxy cho số hộ gia đình / thuê bao hoạt động.
- **`unique_devices`**: Khác với `unique_contracts` vì 1 contract có thể liên kết nhiều MAC.
- **`total_hours`**: Chỉ số nghiệp vụ (Business metric) chính để đo lường mức độ tương tác (engagement) và quy hoạch công suất (capacity planning).
- **`avg_minutes_per_session`**: Chú ý phân phối lệch phải (right-skewed heavy) (mean ~259 phút, median ~112 phút). Dùng để theo dõi xu hướng (trend), không đại diện cho "người dùng thông thường".

**dbt model logic (tham khảo):**

```sql
SELECT
    {{ dbt_utils.generate_surrogate_key(['batch_date', 'app_name']) }} AS pk,
    batch_date,
    app_name,
    COUNT(*)                                           AS total_sessions,
    COUNT(DISTINCT contract_id)                        AS unique_contracts,
    COUNT(DISTINCT device_mac)                         AS unique_devices,
    SUM(total_duration_seconds)                        AS total_seconds,
    ROUND(SUM(total_duration_seconds) / 3600.0, 2)     AS total_hours,
    ROUND(AVG(total_duration_seconds) / 60.0, 2)       AS avg_minutes_per_session
FROM {{ ref('stg_viewing_history') }}
GROUP BY batch_date, app_name
```

---

## Dòng chảy dữ liệu của cột qua các tầng (Cross-Layer Column Lineage)

```
Landing (JSON)          Bronze              Silver              Gold — stg          Gold — fct
─────────────────────────────────────────────────────────────────────────────────────────────
_id                -->  _id            -->  event_id       -->  event_id            (dropped)
_source.Contract   -->  Contract       -->  contract_id*   -->  contract_id    -->  unique_contracts
_source.Mac        -->  Mac            -->  device_mac†    -->  device_mac     -->  unique_devices
_source.TotalDur.  -->  TotalDuration  -->  total_dur_sec‡ -->  total_dur_sec  -->  total_seconds / total_hours
                                                           -->  total_dur_mins -->  avg_minutes_per_session
_source.AppName    -->  AppName        -->  app_name§      -->  app_name       -->  app_name (dim)
filename           -->  date           -->  batch_date     -->  batch_date     -->  batch_date (dim) / pk
                                                           -->  view_year
                                                           -->  view_month
                                                           -->  view_day
(none)                  (none)              is_fraud       -->  is_fraud_suspect    (not in mart)
                                            glue_proc_at        (dropped at stg)
_index             -->  _index         -->  (dropped)
_type              -->  _type          -->  (dropped)

* đã lọc (filtered): loại bỏ NULL, "0", "PAYTV", IP address, chữ thường (lowercase)
† chuẩn hóa (normalized): dạng XX:XX:XX:XX:XX:XX uppercase; loại bỏ (drop) 28 dòng không phân tích cú pháp (parse) được
‡ đã lọc (filtered): chỉ giữ 0 < x <= 86400; loại bỏ (drop) 72,410 dòng
§ đã lọc (filtered): loại bỏ "BHD"; loại bỏ trùng lặp (dedup) theo event_id
```

---

## Từ điển giá trị (Value Glossary) — `app_name`

| Giá trị | Segment | Mô tả |
|---|---|---|
| `CHANNEL` | Linear TV | Xem kênh truyền hình trực tiếp. Chiếm ~90% tổng số lượng session. |
| `VOD` | On-Demand | Video on Demand — xem nội dung theo yêu cầu (phim, series). |
| `CHILD` | Kids | Nội dung dành cho trẻ em. |
| `KPLUS` | Premium | Gói kênh trả thêm (K+, thể thao, phim quốc tế). |
| `RELAX` | Lifestyle | Kênh nhạc, thiên nhiên, ambient — thường bật nền. |
| `SPORT` | Sports | Nội dung thể thao trực tiếp và phát lại (replay). |
| `FIMS` | On-Demand | Kênh phim (Film Is My Shortcut hoặc tương đương). |
| `BHD` (removed) | Test | 1 bản ghi duy nhất — sự kiện kiểm thử (test event), đã loại bỏ tại Silver. |

---

## Từ điển giá trị (Value Glossary) — `contract_id` Prefix

| Tiền tố (Prefix) (2 ký tự) | Tỉnh / Thành phố | Số bản ghi (tháng 4/2022) |
|---|---|---|
| `HN` | Hà Nội | 10,995,778 |
| `SG` | TP. Hồ Chí Minh | 8,177,226 |
| `DN` | Đà Nẵng | 1,759,581 |
| `HP` | Hải Phòng | 1,741,319 |
| `DA` | Đắk Lắk / Đà Lạt | 1,627,305 |
| `QN` | Quảng Nam / Ninh | 1,605,148 |
| `BD` | Bình Dương | 1,300,542 |
| `HD` | Hải Dương | 1,219,368 |
| `VT` | Bà Rịa – Vũng Tàu | 1,191,000 |
| `NT` | Ninh Thuận | 1,143,778 |

> Tiền tố (Prefix) 3 ký tự (ví dụ HNH, SGH, HNJ) có thể mã hóa (encode) thêm thông tin gói dịch vụ hoặc khu vực con — chưa xác nhận với nghiệp vụ (business). Không dùng làm dimension chính thức cho đến khi có ánh xạ (mapping) chính xác.

---

## Hạn chế đã biết (Known Limitations) & Câu hỏi để ngỏ (Open Questions)

| ID | Vấn đề (Issue) | Trạng thái (Status) |
|---|---|---|
| L-01 | Ngưỡng gian lận (Fraud threshold) cho MAC/contract chưa xác nhận. | Mở (Open) |
| L-02 | Tiền tố (Prefix) 3-4 ký tự trong `contract_id` chưa có ánh xạ (mapping) chính thức. | Mở (Open) |
| L-03 | `avg_minutes_per_session` dùng mean nên bị lệch (skew) bởi outlier. | Đã ghi nhận (Documented)<br>- Cần cảnh báo ở dashboard |
| L-04 | Bản ghi có `duration_seconds = 86,400` chính xác chưa được kiểm đếm. | Mở (Open) |
| L-05 | `glue_processed_at` bị loại bỏ (drop) tại staging. Quản lý dấu vết khó hơn. | Thảo luận (Consider)<br>- Giữ nếu cần SLA tracking |
| L-06 | `fct_daily_views` bao gồm cả nghi ngờ gian lận (fraud suspects). | Đã ghi nhận (Documented)<br>- Downstream tự lọc (filter) |